<ul id="messages">
  <% for(let i = listMessage.length -1 ; i >= 0 ; i--){ %>
  <li><%-listMessage[i].username + " "+ listMessage[i].message %></li>
  <% } %>
</ul>
<form id="form" action="">
  <input id="input" autocomplete="off" /><button>Send</button>
</form>
